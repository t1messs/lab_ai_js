| # | Категорія | Де (файл/рядок) | Проблема | Чому це погано | Як виправити |
|---|----------|------------------|---------|----------------|--------------|
| 1 | Maintainability | `static Map<String, String> cache = new HashMap<>();` | Застарілість | Візуальний шум | Лишити один метод String |
| 2 | Readability | `boolean ok = login(email, password);` | Незрозумілість | Змінна ok занадто абстрактна | Замінити на `isLoggedIn` або іншу більш зрозумілу змінну |
| 3 | Maintainability | `if (ok == true) ` | Надлишкове порівняння | Візуальний шум | Використовувати лаконічний запис: `if (isLoggedIn)` |
| 4 | Architecture | `System.out.println("OK");` | Використання "магічних" рядків для виводу статусу | Незрозуміла відопвідь | Виводити деталізовані повідомлення | 
| 5 | Correctness | `int qty = Integer.parseInt(sc.nextLine());` | Відсутність обробки помилок | Якщо користувач введе текст замість цифри, програма впаде | Додати перевірку | 
| 6 | Maintainability | `String rep = buildReport(lastUserEmail);` | Глобальний стан | Метод залежить від зовнішньої змінної lastUserEmail. Якщо користувач не залогінився, там може бути null, що призведе до помилки. | Перевіряти наявність сесії перед викликом звіту або передавати об'єкт користувача. | 
